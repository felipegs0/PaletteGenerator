export type PaletteType = "triad" | "complementary" | "mono" | "analog";
export type PreviewType = "default" | "form" | "ecommerce";
export type PaletteColors = {
  bg: string;
  surface: string;
  text: string;
  primary: string;
  secondary: string;
};
